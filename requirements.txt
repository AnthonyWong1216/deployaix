# AIX LPAR Deployment Script Dependencies
# Install with: pip install -r requirements.txt

# SSH connection library for HMC access
paramiko>=3.0.0

# YAML configuration file parsing
PyYAML>=6.0

# Optional: For enhanced logging and monitoring
colorlog>=6.7.0

# Optional: For Excel file processing (if using Excel input files)
openpyxl>=3.1.0
pandas>=2.0.0

# Optional: For JSON configuration files
# (included in Python standard library)

# Optional: For web-based configuration interface
flask>=2.3.0
flask-cors>=4.0.0 